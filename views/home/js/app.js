'use strict';
var App = angular.module('campCunApp', []);

App.controller('AppCtrl', ['$scope', 
    function (scope) {
        
    }]);

App.factory('AppF', [
    function () {
        return {
            mainView: "map",
            
        }
    }]);